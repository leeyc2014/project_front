import type { AutoZoomSettings } from "@/types/logisticsMap";

export const DEFAULT_AUTO_ZOOM_SETTINGS: AutoZoomSettings = {
  singlePointZoom: 10.8,
  boundsMaxZoom: 10.8,
  durationMs: 900,
  offsetX: 0,
  offsetY: 0,
};